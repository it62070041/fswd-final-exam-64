apps:
  - script       : 'dist/app.js'
    node_args    : '-r dotenv/config'
    name         : 'app'
    env          :
      NODE_ENV   : production
    max_restarts : 300
    restart_delay: 600000
